rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Team registration data is private and is available only through trusted
    // callable functions using the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

